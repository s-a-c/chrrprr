<?php

declare(strict_types=1);

namespace Database\Seeders;

use Illuminate\Database\Seeder;

final class RoleSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * Note: Roles are primarily created via migrations (e.g., create_super_admin_role_and_system_administrator).
     * This seeder can be used for additional role seeding if needed.
     */
    public function run(): void
    {
        // Roles are created in migrations, so this seeder is intentionally empty.
        // Add additional role seeding logic here if needed.
    }
}
